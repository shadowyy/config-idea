#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
/**
 * ${DESCRIPTION}
 * @author ${USER}
 * @version ${YEAR}-${MONTH}-${DAY} ${HOUR}:${MINUTE}
 */
public class ${NAME} {
    
}
